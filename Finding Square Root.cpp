#include <stdio.h>
#include <math.h>

int main()
{
    int x;
    double res;
    printf(" Enter x value: ");
    scanf("%d", &x);
    res = sqrt(x);
    printf("The square root is %f", res);
    return 0;
}
